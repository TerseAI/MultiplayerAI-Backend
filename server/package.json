{
  "name": "@terse/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --import tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.0.0",
    "tsx": "^4.20.6",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@composio/core": "^0.18.0",
    "express": "^5.2.1",
    "terse-sdk": "file:../../ai-product-owner/packages/terse-sdk",
    "zod": "^4.5.4"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
